# Built frontend (generated by `task frontend` / vite build).
# The whole dist is ignored; a placeholder index.html is force-committed so
# `go build` (go:embed frontend/dist) works on a fresh checkout.
/frontend/dist/

# Wails / Go build output
/bin/
*.exe
*.app
*.syso

# Local px binaries if copied in for testing
/px
/px-service
